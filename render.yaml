services:
  - type: web
    name: stud-poker
    runtime: python
    plan: free
    buildCommand: python3 -m compileall stud_sim
    startCommand: python3 -m stud_sim.http_server --host 0.0.0.0 --port $PORT
    autoDeploy: true
